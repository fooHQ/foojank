var app = cli.app({
    name: "cat",
    usage: "Concatenate and print files",
    hide_help_command: true,
    action: func(c) {
        for _, arg := range c.args() {
            print(cat(arg))
        }
    }
})

func main() {
    app.run(os.args())
}
