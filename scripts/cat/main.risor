var app = cli.app({
    name: "cat",
    usage: "Concatenate and print files",
    action: func(c) {
        for _, arg := range c.args() {
            print(cat(arg))
        }
    }
})

func main() {
    app.run(os.args())
}
